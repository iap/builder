#!/usr/bin/env bash
# Convenience shim: run the aws-build CLI from anywhere.
# Usage: aws-build login | status | whoami | logout | models
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "${HERMES_AGENT_PYTHON:-python3}" "$SCRIPT_DIR/aws_build_cli.py" "$@"
