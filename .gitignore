# Secrets + transient auth state — never commit
.bid_token.json
.bid_registration.json
.bid_flow.json
.q_token.json
__pycache__/
*.pyc
.pytest_cache/

# OIDC public client secret (AWS published, not private)
auth/oidc_client_secret.json
