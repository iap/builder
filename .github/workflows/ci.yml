name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@f548e57e544e1ff5a4c46bf1e1b8685f8e4a348a
      - uses: actions/checkout@f548e57e544e1ff5a4c46bf1e1b8685f8e4a348a
        with:
          repository: iap/hermes-agent
          path: hermes-agent
      - uses: astral-sh/setup-uv@094aa226ed0e8f079adf9b0803105ffdac7bb465
        with:
          version: "0.5.x"
      - run: uv --version
      - run: uv tool install ruff@0.16
      - run: ruff --version
      - name: Install deps
        run: uv sync --extra dev
      - name: Lint
        run: ruff check . --exclude hermes-agent
      - name: Format
        run: ruff format --check . --exclude hermes-agent
      - name: Tests
        env:
          HERMES_HOME: ${{ github.workspace }}
          PYTHONPATH: ${{ github.workspace }}/hermes-agent
        run: uv run pytest -x -q tests

  verify:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@f548e57e544e1ff5a4c46bf1e1b8685f8e4a348a
      - uses: astral-sh/setup-uv@094aa226ed0e8f079adf9b0803105ffdac7bb465
        with:
          version: "0.5.x"
      - run: uv --version
      - name: Install deps
        run: uv sync --extra dev
      - name: Verify
        run: uv run python verify.py

  integration-test:
    runs-on: ubuntu-latest
    needs: verify
    steps:
      - uses: actions/checkout@f548e57e544e1ff5a4c46bf1e1b8685f8e4a348a
        with:
          repository: iap/hermes-agent
          path: hermes-agent
      - uses: actions/checkout@f548e57e544e1ff5a4c46bf1e1b8685f8e4a348a
        with:
          path: plugin-src
      - uses: astral-sh/setup-uv@094aa226ed0e8f079adf9b0803105ffdac7bb465
        with:
          version: "0.5.x"
      - run: uv --version
      - run: uv tool install ruff@0.16
      - run: ruff --version
      - name: Install deps
        run: |
          set -euo pipefail
          HM="${{ github.workspace }}/hm"
          mkdir -p "$HM/plugins"
          ln -s "${{ github.workspace }}/plugin-src" "$HM/plugins/builder"
          echo "HERMES_HOME=$HM" >> "$GITHUB_ENV"
          cd "${{ github.workspace }}/plugin-src"
          uv sync --extra dev
      - name: Integration tests
        env:
          HERMES_HOME: ${{ env.HERMES_HOME }}
          PYTHONPATH: ${{ github.workspace }}/hermes-agent
        run: |
          set -euo pipefail
          cd "${{ github.workspace }}/plugin-src"
          ruff check . --exclude hermes-agent
          ruff format --check . --exclude hermes-agent
          uv run pytest -x -q tests/integration
