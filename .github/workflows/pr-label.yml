name: PR auto-label by branch prefix

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7, pinned to full SHA (supply-chain hardening)
        with:
          script: |
            const branch = context.payload.pull_request.head.ref
            const labels = {
              'fix/': 'bug',
              'bugs/': 'bug',
              'feature/': 'enhancement'
            }
            const mapping = Object.entries(labels).find(([prefix]) => branch.startsWith(prefix))
            if (!mapping) return
            const [, label] = mapping
            await github.rest.issues.addLabels({
              ...context.repo,
              issue_number: context.issue.number,
              labels: [label]
            })
